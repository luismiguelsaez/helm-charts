apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluxcd-bluegreen.fullname" . }}-test-connection"
  labels:
    {{- include "fluxcd-bluegreen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluxcd-bluegreen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
