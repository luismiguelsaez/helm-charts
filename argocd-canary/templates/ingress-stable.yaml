{{- if .Values.ingress.stable.enabled -}}
{{- $fullName := include "argocd-canary.fullname" . -}}
{{- $svcPort := .Values.service.port -}}
{{- if and .Values.ingress.stable.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress.stable.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress.stable.annotations "kubernetes.io/ingress.class" .Values.ingress.stable.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: "{{ $fullName }}-stable"
  labels:
    {{- include "argocd-canary.labels" . | nindent 4 }}
  {{- with .Values.ingress.stable.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress.stable.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingress.stable.className }}
  {{- end }}
  {{- if .Values.ingress.stable.tls }}
  tls:
    {{- range .Values.ingress.stable.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.stable.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: "{{ $fullName }}-stable"
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: "{{ $fullName }}-stable"
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
