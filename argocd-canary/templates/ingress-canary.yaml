{{- if .Values.ingress.canary.enabled -}}
{{- $fullName := include "argocd-canary.fullname" . -}}
{{- $svcPort := .Values.service.port -}}
{{- if and .Values.ingress.canary.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress.canary.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress.canary.annotations "kubernetes.io/ingress.class" .Values.ingress.canary.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: "{{ $fullName }}-canary"
  labels:
    {{- include "argocd-canary.labels" . | nindent 4 }}
  {{- with .Values.ingress.canary.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress.canary.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingress.canary.className }}
  {{- end }}
  {{- if .Values.ingress.canary.tls }}
  tls:
    {{- range .Values.ingress.canary.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.canary.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: "{{ $fullName }}-canary"
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: "{{ $fullName }}-canary"
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
